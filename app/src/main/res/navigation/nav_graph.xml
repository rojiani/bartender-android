<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/searchFragment">

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.nrojiani.bartender.views.SearchFragment"
        android:label="search_fragment"
        tools:layout="@layout/search_fragment">
        <action
            android:id="@+id/action_searchFragment_to_cocktailFragment"
            app:destination="@id/cocktailFragment" />
    </fragment>

    <fragment
        android:id="@+id/cocktailFragment"
        android:name="com.nrojiani.bartender.views.CocktailFragment"
        android:label="cocktail_fragment"
        tools:layout="@layout/cocktail_fragment">
        <argument
            android:name="cocktailName"
            app:argType="string" />
    </fragment>
</navigation>